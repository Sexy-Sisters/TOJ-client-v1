export interface IInitialSchool {
  code: string;
  grade: string;
}

export interface IJoin {
  schoolCode: string;
}

export interface IStudentCard {
  age: number;
  grade: number;
  classroom: number;
  number: number;
}
